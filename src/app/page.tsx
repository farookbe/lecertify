import { prisma } from '@/lib/db';
export default async function Dashboard(){ const [total,issued,failed]=await Promise.all([prisma.certificate.count(),prisma.certificate.count({where:{status:'ISSUED'}}),prisma.certificate.count({where:{status:'FAILED'}})]); return <div className="space-y-6"><h1 className="text-3xl font-bold">Certificate Dashboard</h1><div className="grid gap-4 md:grid-cols-3"><Metric label="Total" value={total}/><Metric label="Issued" value={issued}/><Metric label="Failed" value={failed}/></div><a className="btn" href="/certificates/new">New certificate</a></div> } function Metric(p:{label:string;value:number}){return <div className="card p-5"><div className="text-sm text-slate-500">{p.label}</div><div className="text-3xl font-bold">{p.value}</div></div>}
