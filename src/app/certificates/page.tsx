import { prisma } from '@/lib/db';
export default async function Certificates(){ const certs=await prisma.certificate.findMany({orderBy:{updatedAt:'desc'}}); return <div className="space-y-6"><div className="flex items-center justify-between"><h1 className="text-3xl font-bold">Certificates</h1><a className="btn" href="/certificates/new">New certificate</a></div><div className="card overflow-hidden"><table className="w-full text-sm"><tbody>{certs.map(c=><tr className="border-t" key={c.id}><td className="p-3 font-medium">{c.commonName}</td><td>{c.status}</td><td><a className="btn-secondary" href={`/certificates/${c.id}`}>Open</a></td></tr>)}</tbody></table></div></div> }
