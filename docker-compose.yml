services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: lecertify
      POSTGRES_PASSWORD: lecertify-dev-password
      POSTGRES_DB: lecertify
    ports: ["5432:5432"]
    volumes: ["lecertify_pg:/var/lib/postgresql/data"]
volumes:
  lecertify_pg:
